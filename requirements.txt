# Python 의존성 (pip install -r requirements.txt)

# HTTP 요청
requests>=2.31.0

# 이미지 생성 (PIL/Pillow)
Pillow>=10.0.0

# TTS
gTTS>=2.4.0

# 환경변수 관리
python-dotenv>=1.0.0

# Google Trends 실시간 연구 (트렌드 주제 선택)
# pytrends 4.9.2 + urllib3 2.x 충돌 패치: urllib3를 1.x로 고정
pytrends>=4.9.2
urllib3<2.0
